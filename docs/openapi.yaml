components:
  schemas:
    Account:
      additionalProperties: false
      properties:
        age:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        doctor_id:
          type: string
        email:
          type: string
        first_name:
          type: string
        hospital_id:
          type: string
        id:
          type: string
        last_name:
          type: string
        status:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - id
        - first_name
        - last_name
        - age
        - email
        - status
        - doctor_id
        - hospital_id
        - created_at
        - updated_at
      type: object
    CreateAccountInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateAccountInputBody.json
          format: uri
          readOnly: true
          type: string
        age:
          format: int64
          type: integer
        email:
          type: string
        first_name:
          type: string
        last_name:
          type: string
      required:
        - first_name
        - last_name
        - age
        - email
      type: object
    CreateAccountResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateAccountResponseBody.json
          format: uri
          readOnly: true
          type: string
        account:
          $ref: "#/components/schemas/Account"
      required:
        - account
      type: object
    CreateDoctorInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateDoctorInputBody.json
          format: uri
          readOnly: true
          type: string
        account_id:
          type: string
      required:
        - account_id
      type: object
    CreateDoctorResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateDoctorResponseBody.json
          format: uri
          readOnly: true
          type: string
        doctor:
          $ref: "#/components/schemas/Doctor"
      required:
        - doctor
      type: object
    CreateDoctorToAccountAssignmentInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateDoctorToAccountAssignmentInputBody.json
          format: uri
          readOnly: true
          type: string
        account_id:
          type: string
        doctor_id:
          type: string
      required:
        - account_id
        - doctor_id
      type: object
    CreateDoctorToAccountAssignmentResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateDoctorToAccountAssignmentResponseBody.json
          format: uri
          readOnly: true
          type: string
        assignment:
          $ref: "#/components/schemas/DoctorAssignment"
      required:
        - assignment
      type: object
    CreateHospitalInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateHospitalInputBody.json
          format: uri
          readOnly: true
          type: string
        name:
          type: string
      required:
        - name
      type: object
    CreateHospitalResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateHospitalResponseBody.json
          format: uri
          readOnly: true
          type: string
        hospital:
          $ref: "#/components/schemas/Hospital"
      required:
        - hospital
      type: object
    CreateMedicalRecordInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateMedicalRecordInputBody.json
          format: uri
          readOnly: true
          type: string
        account_id:
          type: string
        description:
          type: string
        title:
          type: string
      required:
        - account_id
        - title
        - description
      type: object
    CreateMedicalRecordResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateMedicalRecordResponseBody.json
          format: uri
          readOnly: true
          type: string
        medical_record:
          $ref: "#/components/schemas/MedicalRecord"
      required:
        - medical_record
      type: object
    CreateNurseInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateNurseInputBody.json
          format: uri
          readOnly: true
          type: string
        account_id:
          type: string
      required:
        - account_id
      type: object
    CreateNurseResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateNurseResponseBody.json
          format: uri
          readOnly: true
          type: string
        nurse:
          $ref: "#/components/schemas/Nurse"
      required:
        - nurse
      type: object
    CreateNurseToHospitalAssignmentInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateNurseToHospitalAssignmentInputBody.json
          format: uri
          readOnly: true
          type: string
        hospital_id:
          type: string
        nurse_id:
          type: string
      required:
        - nurse_id
        - hospital_id
      type: object
    CreateNurseToHospitalAssignmentResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/CreateNurseToHospitalAssignmentResponseBody.json
          format: uri
          readOnly: true
          type: string
        assignment:
          $ref: "#/components/schemas/NurseToHospitalAssignment"
      required:
        - assignment
      type: object
    Doctor:
      additionalProperties: false
      properties:
        account_id:
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        status:
          type: string
      required:
        - id
        - account_id
        - status
        - created_at
      type: object
    DoctorAssignment:
      additionalProperties: false
      properties:
        account_id:
          type: string
        doctor_id:
          type: string
        id:
          type: string
        valid_from:
          format: date-time
          type: string
        valid_to:
          format: date-time
          type:
            - string
            - "null"
      required:
        - id
        - account_id
        - doctor_id
        - valid_from
        - valid_to
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    GetAccountByIDResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/GetAccountByIDResponseBody.json
          format: uri
          readOnly: true
          type: string
        account:
          $ref: "#/components/schemas/Account"
      required:
        - account
      type: object
    HealthcheckBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/HealthcheckBody.json
          format: uri
          readOnly: true
          type: string
        status:
          description: Service health status
          type: string
      required:
        - status
      type: object
    Hospital:
      additionalProperties: false
      properties:
        created_at:
          format: date-time
          type: string
        id:
          type: string
        name:
          type: string
        status:
          type: string
      required:
        - id
        - name
        - status
        - created_at
      type: object
    ListAccountsResponseBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/ListAccountsResponseBody.json
          format: uri
          readOnly: true
          type: string
        accounts:
          items:
            $ref: "#/components/schemas/Account"
          type:
            - array
            - "null"
      required:
        - accounts
      type: object
    MedicalRecord:
      additionalProperties: false
      properties:
        account_id:
          type: string
        created_at:
          format: date-time
          type: string
        description:
          type: string
        id:
          type: string
        status:
          type: string
        title:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - id
        - account_id
        - title
        - description
        - status
        - created_at
        - updated_at
      type: object
    Nurse:
      additionalProperties: false
      properties:
        account_id:
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        status:
          type: string
      required:
        - id
        - account_id
        - status
        - created_at
      type: object
    NurseToHospitalAssignment:
      additionalProperties: false
      properties:
        hospital_id:
          type: string
        id:
          type: string
        nurse_id:
          type: string
        valid_from:
          format: date-time
          type: string
        valid_to:
          format: date-time
          type:
            - string
            - "null"
      required:
        - id
        - nurse_id
        - hospital_id
        - valid_from
        - valid_to
      type: object
    UpdateAccountInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - http://localhost:/schemas/UpdateAccountInputBody.json
          format: uri
          readOnly: true
          type: string
        age:
          format: int64
          type: integer
        email:
          type: string
        first_name:
          type: string
        last_name:
          type: string
      required:
        - first_name
        - last_name
        - age
        - email
      type: object
info:
  title: OpenHealth API
  version: 1.0.0
openapi: 3.1.0
paths:
  /api/v1/accounts:
    get:
      description: Lists accounts up to the provided limit
      operationId: list-accounts
      parameters:
        - explode: false
          in: query
          name: limit
          schema:
            default: 10
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListAccountsResponseBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/accounts - [GET]
      tags:
        - accounts
    post:
      description: Creates a new account
      operationId: create-account
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAccountInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateAccountResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/accounts - [POST]
      tags:
        - accounts
  /api/v1/accounts/{accountID}:
    delete:
      description: Archives an existing account
      operationId: archive-account
      parameters:
        - in: path
          name: accountID
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/accounts/{accountID} - [DELETE]
      tags:
        - accounts
    get:
      description: Gets an account by its ID
      operationId: get-account
      parameters:
        - in: path
          name: accountID
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetAccountByIDResponseBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/accounts/{accountID} - [GET]
      tags:
        - accounts
    put:
      description: Updates an existing account
      operationId: update-account
      parameters:
        - in: path
          name: accountID
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountInputBody"
        required: true
      responses:
        "200":
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/accounts/{accountID} - [PUT]
      tags:
        - accounts
  /api/v1/doctors:
    post:
      description: Creates a new doctor
      operationId: create-doctor
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDoctorInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDoctorResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/doctors - [POST]
      tags:
        - doctors
  /api/v1/doctors/assignments:
    post:
      description: Assigns a doctor to an account
      operationId: create-doctor-to-account-assignment
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDoctorToAccountAssignmentInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateDoctorToAccountAssignmentResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/doctors/assignments - [POST]
      tags:
        - doctors
  /api/v1/healthcheck:
    get:
      description: Checks if the service is healthy
      operationId: healthcheck
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthcheckBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/healthcheck - [GET]
      tags:
        - system
  /api/v1/hospitals:
    post:
      description: Creates a new hospital
      operationId: create-hospital
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHospitalInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateHospitalResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/hospitals - [POST]
      tags:
        - hospitals
  /api/v1/medical-records:
    post:
      description: Creates a new medical record
      operationId: create-medical-record
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMedicalRecordInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateMedicalRecordResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/medical-records - [POST]
      tags:
        - medical-records
  /api/v1/nurses:
    post:
      description: Creates a new nurse
      operationId: create-nurse
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNurseInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateNurseResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/nurses - [POST]
      tags:
        - nurses
  /api/v1/nurses/assignments:
    post:
      description: Assigns a nurse to a hospital
      operationId: create-nurse-to-hospital-assignment
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNurseToHospitalAssignmentInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateNurseToHospitalAssignmentResponseBody"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      summary: /api/v1/nurses/assignments - [POST]
      tags:
        - nurses
servers:
  - url: "http://localhost:"

