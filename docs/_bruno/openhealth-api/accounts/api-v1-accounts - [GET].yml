info:
  name: /api/v1/accounts - [GET]
  type: http
  seq: 1
  tags:
    - accounts

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/accounts"
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{BASE_URL}}/api/v1/accounts"
      method: GET
      params:
        - name: limit
          value: "10"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "$schema": "",
            "accounts": ""
          }
  - name: default Response
    description: Error
    request:
      url: "{{BASE_URL}}/api/v1/accounts"
      method: GET
      params:
        - name: limit
          value: "10"
          type: query
    response:
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: |-
          {
            "$schema": "",
            "detail": "",
            "errors": "",
            "instance": "",
            "status": 0,
            "title": "",
            "type": ""
          }

docs: Lists accounts up to the provided limit
